import 'package:flutter/material.dart';

class CourtFavorite extends StatelessWidget {
  const CourtFavorite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('favorite court'),
      ),
    );
  }
}
