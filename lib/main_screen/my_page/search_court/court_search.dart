import 'package:flutter/material.dart';

class CourtSearch extends StatelessWidget {
  const CourtSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('search court'),
      ),
    );
  }
}
