import 'package:flutter/material.dart';

class CourtKyungki extends StatelessWidget {
  const CourtKyungki({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('경기 코트')
      ),
    );
  }
}
